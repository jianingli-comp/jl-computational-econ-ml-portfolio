## Data

Datasets used in these projects (TPC-H, spam classification data,
Wikipedia/Gnutella graphs) are not included in this public repository.

They are either:
- course-provided datasets, or
- large-scale cluster-resident data.

For reproducibility, scripts and commands in each subproject assume
data paths provided via command-line arguments.
